<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid Margin="12">
        <Grid.Resources>
            <Style TargetType="Label" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogLabelStyleKey}}" />
            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogTextBoxStyleKey}}" />
            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogCheckBoxStyleKey}}" />
            <Style TargetType="Button" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogButtonStyleKey}}" />
            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static styles:VsResourceKeys.ThemedDialogComboBoxStyleKey}}" />
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Ollama Settings" FontWeight="Bold" Margin="0,0,0,12" FontSize="14"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Label Content="Base URL" Margin="0,0,0,4"/>
            <TextBox Text="{Binding BaseUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="280"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <Label Content="Access Token (optional)" Margin="0,0,0,4"/>
            <TextBox Text="{Binding AccessToken, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="280"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <Label Content="Primary Model" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding AvailableModels}" SelectedItem="{Binding SelectedModel, Mode=TwoWay}" MinWidth="220"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <Label Content="Chat Model" Margin="0,0,0,4"/>
            <ComboBox ItemsSource="{Binding AvailableModels}" SelectedItem="{Binding SelectedChatModel, Mode=TwoWay}" MinWidth="220"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,0,0,12" Orientation="Horizontal">
            <StackPanel Margin="0,0,12,0">
                <Label Content="Default Temperature" Margin="0,0,0,4"/>
                <TextBox Text="{Binding DefaultTemperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <Label Content="Chat Temperature" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ChatTemperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
            </StackPanel>
            <StackPanel Margin="0,0,12,0">
                <Label Content="Max Tokens" Margin="0,0,0,4"/>
                <TextBox Text="{Binding MaxOutputTokens, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
            </StackPanel>
            <StackPanel>
                <Label Content="Chat Context" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ChatContextWindow, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="90"/>
            </StackPanel>
        </StackPanel>

        <CheckBox Grid.Row="6" Content="Include current selection by default" IsChecked="{Binding IncludeSelectionByDefault, Mode=TwoWay}" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal">
            <Button Content="Save Settings" Command="{Binding SaveCommand}" Width="100" Margin="0,0,8,0"/>
            <Button Content="Refresh Models" Command="{Binding RefreshModelsCommand}" Width="110" Margin="0,0,8,0"/>
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Width="115" />
            <Label Content="{Binding Status}" Margin="12,4,0,0" FontStyle="Italic"/>
        </StackPanel>
    </Grid>
</DataTemplate>
